{
  "name": "myraha-status-helper",
  "private": true,
  "version": "0.3.0",
  "type": "module",
  "scripts": {
    "build": "vite build && node scripts/inject-api-host.mjs",
    "dev": "vite build --watch",
    "pack": "npm run build && node scripts/pack.mjs",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.1",
    "typescript": "^5.9.2",
    "vite": "^7.1.4"
  }
}
